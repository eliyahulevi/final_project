how to use:
1. clone to your machine "master" branch, let's call it PROJ_DIR
2. open cmd
3. navigate to PROJ_DIR (where pom.xml exist)
4. type "mvn clean packsge"
5. start eclipse
6. file->import->maven project
7. browse to PROJ_DIR and hit enter
8. right click on project and choose "run as" -> "run on server"
9. if no "server" exist, choose tomcat server version 9.0 and in "installation directory" go to TOMCAT_DIR\bin
